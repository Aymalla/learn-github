@model LibraryManagementWebApp.Models.Book

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<link rel="stylesheet" href="/css/site.css">
<script src="/js/modal-handlers.js" defer></script>

<div class="container mt-5">
    <h2 class="text-center">Add New Book</h2>
    <form asp-action="Create" method="post" class="mt-4">
        @Html.AntiForgeryToken()
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="mb-3">
            <label for="Title" class="form-label">Title</label>
            <input type="text" id="Title" name="Title" class="form-control" value="@Model?.Title" required />
            <span asp-validation-for="Title" class="text-danger"></span>
        </div>
        <div class="mb-3">
            <label for="Author" class="form-label">Author</label>
            <input type="text" id="Author" name="Author" class="form-control" value="@Model?.Author" required />
            <span asp-validation-for="Author" class="text-danger"></span>
        </div>
        <div class="mb-3">
            <label for="ISBN" class="form-label">ISBN</label>
            <input type="text" id="ISBN" name="ISBN" class="form-control" value="@Model?.ISBN" />
            <span asp-validation-for="ISBN" class="text-danger"></span>
        </div>
        <div class="form-check mb-3">
            @Html.CheckBoxFor(model => model.IsAvailable, new { @class = "form-check-input" })
            <label class=" form-check-label" for="IsAvailable">Available</label>
        </div>
         <div class="mb-3">
            <button type="submit" class="btn btn-primary">Add Book</button>
            <button type="button" class="btn btn-secondary" onclick="window.location.href='/Books/Index'">Cancel</button>
        </div>
        
    </form>
</div>

@section Scripts {
ipt src="https://cdnjs.cloudflare.com/ajax/libs/jquery-validate/1.19.5/jquery.validate.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-validation-unobtrusive/3.2.13/jquery.validate.unobtrusive.min.js"></script>
}

